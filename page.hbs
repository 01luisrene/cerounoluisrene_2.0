{{!< default}}
{{#post}}
<header class="main-header post-head {{#if image}}" style="background-image: url({{image}}){{else}} no-cover{{/if}}">
    {{navigation}}
</header>
<main class="main" role="main">
<div class="contenedor___post">
  <article class="{{post_class}}">
    <header class="page-header">
      <h1 class="post-titulo">{{title}}</h1>
      <div class="box-time flex wrap aicenter">
        <span class="icon-clock"></span>
        <time class="time" datetime="{{date format="YYYY-MM-DD"}}"> {{date format="DD MMMM YYYY"}}</time>
      </div>
    </header>
    <section class="post_contenido">
      <div class="addthis_sharing_toolbox compartir-redes-sociales"></div>
      {{content}}
    </section>
  </article>
</div>
</main>
{{/post}}