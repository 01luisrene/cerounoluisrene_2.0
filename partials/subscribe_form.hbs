<form method="post" action="{{action}}" class="formulario__suscripcion {{form_class}}">
    {{! This is required for the form to work correctly }}
    {{hidden}}

    <div class="form-group{{#if error}} error{{/if}}">
        {{input_email class=input_class placeholder="Ingresa tu correo electrónico" value=email autofocus=autofocus}}
    </div>
    <button class="btn btn-green {{button_class}}" type="submit">Suscribirse</button>

    {{! This is used to get extra info about where this subscriber came from }}
    {{script}}
</form>

{{#if error}}
    <p class="main-error">{{{error.message}}}</p>
{{/if}}